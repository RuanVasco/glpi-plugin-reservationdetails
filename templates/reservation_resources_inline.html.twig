{% if resources|length > 0 %}
<div class="card mt-3">
    <div class="card-header">
        <i class="fas fa-boxes me-2"></i>
        Recursos disponíveis
    </div>
    <div class="card-body">
        <div class="row">
            {% for resource in resources %}
                <div class="col-md-4 mb-2">
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" 
                               id="resource_id_{{ resource.id }}" 
                               name="resource_id_{{ resource.id }}" 
                               value="{{ resource.id }}" />
                        <label class="form-check-label" for="resource_id_{{ resource.id }}">
                            {{ resource.name }}
                            {% if resource.stock is not null %}
                                <small class="text-muted">({{ resource.stock }} disponíveis)</small>
                            {% endif %}
                        </label>
                    </div>
                </div>
            {% endfor %}
        </div>
        <small class="text-muted mt-2 d-block">
            <i class="fas fa-info-circle me-1"></i>
            Para reservas recorrentes, os recursos serão aplicados a todas as datas. Conflitos de estoque serão ignorados com aviso.
        </small>
    </div>
</div>
{% endif %}
